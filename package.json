{
  "name": "aws-serverless-training",
  "version": "0.1.0",
  "bin": {
    "aws-serverless-training": "bin/aws-serverless-training.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "outputs": "aws cloudformation describe-stacks --stack-name dev-AwsServerlessTrainingPipelineStack | jq '.Stacks | .[] | .Outputs | reduce .[] as $i ({}; .[$i.OutputKey] = $i.OutputValue)'"
  },
  "devDependencies": {
    "@types/jest": "27.5.0",
    "@types/node": "10.17.27",
    "@types/prettier": "2.6.0",
    "jest": "27.5.1",
    "ts-jest": "27.1.4",
    "ts-node": "10.7.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "source-map-support": "0.5.21"
  }
}
